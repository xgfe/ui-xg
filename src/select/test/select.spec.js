describe('ui.fugu.select', function () {
    //var compile,
    //    scope;

    beforeEach(function () {
        module('ui.fugu.select');
        module('select/templates/select.html');
        //inject(function( $compile, $rootScope) {
        //    compile = $compile;
        //    scope = $rootScope.$new();
        //});
    });
    afterEach(function() {

    });

    it('should run without an error',function(){

    });

});