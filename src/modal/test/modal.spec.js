describe('ui.fugu.modal', function () {
    //var compile,
    //    scope;

    beforeEach(function () {
        module('ui.fugu.modal');
        module('modal/templates/modal.html');
        //inject(function( $compile, $rootScope) {
        //    compile = $compile;
        //    scope = $rootScope.$new();
        //});
    });
    afterEach(function() {

    });

    it('should run without an error',function(){

    });

});